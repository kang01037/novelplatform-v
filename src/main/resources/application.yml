server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/novelplatform?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 522051
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: novelplatform

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: org.example.novelplatform.entity